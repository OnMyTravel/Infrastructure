[Unit]
Description=MongoDB
Documentation=man:mongod(1)
After=network.target

[Service]
User={{ remote_user }}
Group={{ remote_user }}
ExecStart=/usr/bin/mongod --quiet --config {{ mongod_conf }}
Restart=always

[Install]
WantedBy=multi-user.target
